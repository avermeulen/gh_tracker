<h1>Coders</h1>

<table class="table">
    <tr>
        <th>
            First name
        </th>
        <th>
            Last name
        </th>
        <th>
            Term
        </th>
    </tr>
    {{#coders}}
    <tr>
        <td>
            {{firstName}}
        </td>
        <td>
            {{lastName}}
        </td>
        <td>
            <form class="" action="/coders/update_term/{{id}}" method="post">

                <select class="" name="term">
                    {{#terms}}
                        <option value="{{this.name}}" {{this.selected}}>{{this.name}}</option>
                    {{/terms}}
                </select>
                <input type="submit" name="name" value="Submit">
            </form>
        </td>
    </tr>
    {{/coders}}
</table>
<script type="text/javascript">

    var termSelect = document.querySelector('.term');
    termSelect.addEventListener('onchange', function(){
        alert("go!");
    });


</script>
